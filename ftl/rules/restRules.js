// Regras de repouso (ORO.FTL.235).
export const REST_FLOOR_BASE_MIN = 720; // 12 h — repouso mínimo na base (235a)
export const REST_FLOOR_AWAY_MIN = 600; // 10 h — repouso mínimo fora da base (235b)

// Repouso de recuperação prolongado recorrente (235d).
export const RECOVERY = { minHours: 36, localNights: 2, maxApartHours: 168 };
